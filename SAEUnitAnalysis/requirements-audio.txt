-r requirements.txt
jiwer>=4.0
sentencepiece>=0.2
# Clone the official vocoder implementation (it is a source tree, not a
# conventional installable package) and pass --bigvgan-repo to the renderer:
#   git clone https://github.com/NVIDIA/BigVGAN.git
